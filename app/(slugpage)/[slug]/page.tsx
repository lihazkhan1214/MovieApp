

import MovieReview from "@/components/Overview";

export default function Page() {
  // Decode the slug to get the original value
  // const decodedSlug = decodeURIComponent(params.slug);
  return (
    <div>
     
      <MovieReview />
    </div>
  );
}
